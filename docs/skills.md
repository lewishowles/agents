# Skills

Skills are focused instruction sets for a specific kind of work. Each combines `skill.json` metadata with a `SKILL.body.md` guide; `scripts/sync.sh` builds the runtime files used by Claude and Codex and the reference tables in this repository. The `agent-config` maintenance skill stays repo-local, so it is not installed for other projects.

<!-- BEGIN GENERATED: user-skills -->
<!-- Generated by scripts/build/build-docs.py. Edit skill.json or hook.json, then run scripts/sync.sh. -->
| Skill | When to use | Auto-trigger keywords |
| ----- | ----------- | --------------------- |
| `accessibility` | When building interfaces, WCAG AA baseline, accessible design | `accessible`, `accessibility`, `wcag`, `a11y`, `aria`, `aria-`, `color contrast`, `colour contrast`, `keyboard nav`, `screen reader`, `focus trap`, `focus management`, `focus ring`, `focus visible`, `tabindex`, `role=`, `alt text`, `caption`, `landmark` |
| `accessibility-audit` | When conducting an accessibility audit of a page, component, or PR; preparing a client report | `audit for accessibility`, `accessibility audit`, `a11y check`, `wcag compliance`, `is this accessible`, `accessibility report` |
| `bash` | When writing shell scripts, bash config, patterns | `.sh`, `bash`, `zsh`, `shell script`, `shell function`, `shell util`, `#!/`, `.env`, `alias`, `.bashrc`, `.zshrc`, `cron`, `makefile` |
| `boilersuit` | When using Boilersuit generators for repeatable file generation | `boilersuit generate`, `boilersuit project inspect`, `use generator`, `run generator`, `generate files`, `scaffold`, `scaffolding`, `boilerplate`, `repeatable file`, `generator preview` |
| `boilersuit-generator-authoring` | When creating or editing a Boilersuit generator definition | `create boilersuit generator`, `new boilersuit generator`, `author generator`, `generator.json`, `.boilersuit/generators`, `generator template`, `custom generator`, `edit generator definition` |
| `code-lookup` | When choosing a code discovery, tracing, health analysis, or semantic editing tool | `where is`, `find code`, `find callers`, `who calls`, `call chain`, `codebase structure`, `understand architecture`, `impact analysis`, `dead code`, `unused exports`, `code duplication`, `complexity hotspots`, `architecture boundaries`, `rename symbol` |
| `code-review` | When reviewing a PR, a diff, existing code at a named path, or receiving review feedback | `review`, `code review`, `pr review`, `pull request review`, `review feedback`, `review this`, `review the` |
| `code-style` | When formatting or organising code, naming things, or writing code comments | (manual only) |
| `codex-insights` | When generating a Codex usage or interaction report | `Codex insights`, `Codex usage report`, `Codex session report`, `latest-narrative.json`, `latest-authoring-bundle.json`, `latest-authored-draft.json`, `codex_insights_extract.py`, `codex_insights_facets.py`, `codex_insights_author.py`, `codex_insights_render.py` |
| `component-api-design` | When creating a component or changing its public props, slots, emits, models, expose, or contract | `component api`, `component public api`, `new component`, `public component`, `component contract`, `prop design`, `props design`, `slot design`, `slots design`, `emit design`, `emits design`, `defineProps`, `defineEmits`, `defineExpose`, `defineModel`, `v-model` |
| `component-library` | When choosing, reusing, or wrapping @lewishowles/components APIs, especially when unclear | `@lewishowles/components`, `data table`, `form field`, `ui component`, `component props`, `component slots`, `component example`, `component pattern` |
| `debugging` | When encountering any bug, test failure, or unexpected behaviour — before proposing a fix | `bug`, `test failure`, `unexpected behaviour`, `unexpected behavior`, `not working`, `debug`, `failing`, `why is`, `broken`, `regression` |
| `dependencies` | When adding packages, what to choose, when to add | `package.json`, `npm install`, `npm add`, `bun add`, `yarn add`, `pnpm add`, `install package`, `add library`, `add dependency`, `new library`, `peer dep`, `devdependencies`, `upgrade package`, `package version` |
| `error-handling` | When validating input, graceful fallbacks, error handling | `error handling`, `try-catch`, `try catch`, `validate`, `validation`, `guard let`, `api call`, `api request`, `fetch`, `async function`, `optional chaining`, `nil coalescing`, `throw`, `Result<`, `throws` |
| `friction-review` | When reviewing recurring friction summary entries to propose rule/skill amendments | `friction-review`, `review friction`, `friction patterns`, `friction summary` |
| `frontend-design` | When designing UI or making aesthetic decisions for a public-facing interface | `design`, `visual design`, `aesthetic`, `landing page`, `hero`, `marketing page`, `typography`, `colour palette`, `color palette`, `brand`, `look and feel`, `motion design`, `animation design`, `layout design`, `design system`, `visual identity`, `UI design`, `composition` |
| `frontend-security` | When writing or reviewing client-side code for security: XSS, CSP, auth tokens, secrets hygiene | `xss`, `injection`, `security`, `csp`, `content security policy`, `v-html`, `authentication`, `auth token`, `secrets`, `sensitive data`, `sanitize`, `sanitise`, `csrf` |
| `human-friendly-cli` | When designing, reviewing, or extending CLI commands | `CLI`, `command line`, `subcommand`, `--help`, `--json`, `cli-style`, `terminal output`, `create command`, `add command` |
| `insights-review` | When reviewing or consolidating a rendered insights report | `insights review`, `review insights report`, `Codex insights report review`, `cross-model insights review`, `consolidate insights reviews`, `insights review packet` |
| `library-release` | When preparing a release for a supported @lewishowles library | `library release`, `release library`, `publish package`, `npm publish`, `release components`, `release helpers`, `release cli-style`, `release lint-config`, `release pkg-checks`, `@lewishowles/components release`, `@lewishowles/helpers release`, `@lewishowles/testing release`, `@lewishowles/cli-style release`, `@lewishowles/lint-config release`, `@lewishowles/pkg-checks release`, `version bump`, `semver`, `changelog` |
| `library-update` | When checking a project for updates to supported @lewishowles dependencies | (manual only) |
| `project-audit` | When auditing a project for setup drift, stale generated output, missing diagnostics, command-safety gaps, or agent-readiness issues | (manual only) |
| `project-backport-review` | When comparing a consumer project against the boilerplate template for backport candidates and boilerplate improvements | `compare this with boilerplate`, `backport to boilerplate`, `what can we backport`, `sync boilerplate with this project`, `check boilerplate against this project`, `improve boilerplate based on this project`, `anything in here better than boilerplate` |
| `project-continue` | When resuming work from progress CLI records; checks optional root PROGRESS.md freeform prose only when needed | (manual only) |
| `project-learn-from-source` | When extracting repo-specific lessons from an external artefact | `learn from this`, `learn anything from this`, `anything useful from this`, `what should we copy`, `what can we adopt`, `review this AGENTS.md`, `review this skill repo`, `review this blog post`, `ground this in the repo`, `useful to this repo` |
| `project-plan-task` | When adding planned work or splitting a task into chunks | (manual only) |
| `project-review-commits` | When reviewing committed work across a Git range or since the last tag | `review commits`, `review every commit`, `review since last tag`, `check commits since last tag`, `sanity check commits`, `review commit range`, `review history`, `release sanity check` |
| `project-review-patches` | When explicitly reviewing uncommitted work as refreshable proposed commit patches | `review as patches`, `review patch by patch`, `review proposed commits`, `create review patches`, `split review into patches`, `refresh review patch` |
| `project-review-progress` | When reviewing an existing PROGRESS.md plan or project roadmap | `review progress`, `review progress.md`, `check progress`, `check the plan`, `review the plan`, `is this a good plan`, `progress plan`, `what is missing from the plan`, `improve the plan` |
| `project-review-task` | When assessing or consolidating a task contract before implementation | `review task contract`, `review task record`, `review implementation task`, `assess task readiness`, `check task before implementation`, `planning quality review`, `consolidate task review`, `consolidate planning review`, `merge peer review findings`, `reconcile peer task reviews` |
| `project-review-worktree` | When reviewing uncommitted work before commit | `review worktree`, `review working tree`, `review uncommitted`, `before commit`, `ready to commit`, `check this before commit`, `review these changes`, `review against progress` |
| `project-setup` | When starting a new project or feature; explores the repo, asks questions, and creates progress CLI records before coding | (manual only) |
| `project-synthesise-feedback` | When critically synthesising another agent's suggestions with current recommendations | `what do you think of this`, `another agent suggested`, `suggestions from another agent`, `review these suggestions`, `combine these recommendations`, `synthesise feedback`, `make this stronger`, `compare recommendations`, `second opinion` |
| `refactoring` | When refactoring existing code or triaging technical debt | `refactor`, `technical debt`, `tech debt`, `clean up`, `code smell`, `simplify`, `restructure` |
| `session-retrospective` | When extracting lessons from a difficult agent session | `learn from this session`, `learn from that session`, `session retrospective`, `retrospect on this session`, `retrospect on that session`, `review this difficult session`, `review that difficult session`, `extract lessons from this session`, `what should we learn from this`, `what can we learn from that session` |
| `skill-craft` | When creating, designing, or evaluating a new skill for this repo | `new skill`, `author skill`, `create skill`, `add a skill`, `write a skill`, `skill lifecycle`, `skill eval`, `design a skill` |
| `source-extraction` | When preparing a lossless extraction of an external source for later analysis | `extract this source`, `extraction prompt`, `distill this article`, `distil this article`, `extract raw material`, `prep this for learn-from-source`, `give this to chatgpt`, `summarise without losing meaning`, `pull out the claims from this page` |
| `swift` | When writing Swift, style, SwiftUI patterns, concurrency | `.swift`, `swift`, `swiftui`, `xcode`, `actor`, `struct`, `@observable`, `@state`, `@mainactor`, `@environment`, `@binding`, `nsworkspace`, `dispatchsource`, `macos app`, `appkit`, `uikit`, `swiftdata`, `xctest`, `async await swift` |
| `swift-ui` | When writing/reviewing SwiftUI code, views, state management | `swiftui`, `@observable`, `@bindable`, `@state`, `@mainactor`, `@environment`, `@binding`, `navigationstack`, `navigationsplitview`, `tabview`, `labeledcontent`, `swiftui view`, `view composition` |
| `test` | When deciding what to test and at which layer — strategy above test-unit and test-e2e | `what to test`, `test strategy`, `test pyramid`, `which tests`, `testing approach`, `tdd`, `test-driven` |
| `test-e2e` | When writing end-to-end tests with Playwright or planning a browser bug hunt | `e2e`, `playwright`, `cypress`, `browser automation`, `browser bug hunt`, `find browser bugs`, `break this page`, `try to break this app`, `end-to-end test`, `user journey test`, `data-test` |
| `test-unit` | When writing unit tests, Vitest, philosophy, what to skip | `unit test`, `test`, `spec`, `coverage`, `.test.`, `.spec.`, `xctest`, `vitest`, `describe`, `beforeeach`, `aftereach`, `mock`, `spy`, `stub`, `assert`, `@testing-library`, `snapshot test` |
| `typescript` | When using TypeScript, type safety, escape hatches | `.ts`, `.tsx`, `typescript`, `lang="ts"`, `type error`, `type def`, `interface`, `generics`, `infer`, `keyof`, `typeof`, `Record<`, `Partial<`, `Pick<`, `Omit<`, `type alias`, `union type`, `intersection type`, `type guard`, `as any` |
| `vue` | When writing Vue code, formatting, patterns, composables, components | `.vue`, `vue`, `composable`, `script setup`, `template`, `defineProps`, `defineEmits`, `defineExpose`, `ref(`, `reactive(`, `computed(`, `watch(`, `onMounted(`, `composition api` |
| `vue-pinia` | When using Pinia for client-side Vue app state and stores | `@pinia/testing`, `pinia`, `defineStore`, `storeToRefs`, `useStore`, `client state`, `app state`, `ui state` |
| `vue-pinia-colada` | When using `@pinia/colada` for async server state — `useQuery`, `useMutation`, cache management, optimistic updates | `@pinia/colada`, `pinia colada`, `useQuery`, `useMutation`, `defineQuery`, `defineMutation`, `queryCache`, `invalidateQueries`, `useQueryCache` |
| `vue-project-stack` | When working in Vue + Bun + Vitest + Tailwind + Gitflow stack | `@lewishowles`, `vue-router`, `vitejs`, `vite`, `gitflow`, `tailwind`, `vue project stack`, `lewishowles`, `.vue`, `vue`, `composable`, `script setup`, `defineProps`, `defineEmits`, `defineExpose` |
| `vue-router` | When using Vue Router routes, guards, params, query strings, and redirects | `vue-router`, `vue router`, `router`, `routes`, `useRoute`, `useRouter`, `onBeforeRouteUpdate`, `onBeforeRouteLeave`, `navigation guard`, `beforeEach`, `route params`, `query string`, `redirect` |
| `vue-vite` | When configuring vite.config.ts, Vite project patterns | `vite.config`, `vite.config.ts`, `environment var`, `VITE_`, `build.lib`, `bundle size`, `vite config`, `rollup`, `esbuild`, `dev server` |
| `web-performance` | When optimising Core Web Vitals, bundle size, or asset loading for Vue/Vite/GitHub Pages projects | `performance`, `web vitals`, `lcp`, `cls`, `inp`, `bundle size`, `code splitting`, `lazy load`, `core web vitals`, `lighthouse`, `runtime performance` |
| `web-performance-audit` | When conducting a performance audit of a page, PR, or app; preparing a performance report | `performance audit`, `audit for performance`, `check core web vitals`, `check web vitals`, `is this fast enough`, `performance report` |
| `writing` | When writing prose/documentation, voice, tone, structure, style | `documentation`, `docs`, `blog`, `changelog`, `announcement`, `article`, `reword`, `rephrase`, `wording`, `proofread`, `edit the text`, `edit the copy`, `readme`, `commit message`, `docstring`, `test name`, `handoff`, `report`, `summary` |
| `writing-copy` | When writing microcopy, buttons, errors, empty states, CTAs | `ui copy`, `microcopy`, `error message`, `button label`, `button copy`, `cta`, `empty state`, `tooltip`, `placeholder text`, `confirmation dialog`, `helper text`, `form label`, `ux writing`, `ui strings`, `ui copy consistency`, `user-facing`, `user facing` |
| `writing-readme` | When writing a README, structure, what to include/cut | `readme`, `read.me`, `getting started guide`, `getting started doc` |
<!-- END GENERATED: user-skills -->

## How Claude and Codex find skills

Claude and Codex use the same skill sources, but discovery works differently. Claude's trigger hooks nudge a matching skill into the session when prompt keywords or the file being edited match its patterns. Codex chooses skills from their descriptions, so write an action-led sentence that starts with `Use this skill when...`.

Claude reads shared skills from `~/.claude/skills/`, while Codex reads them from `~/.agents/skills/`. Claude also provides built-in skills managed by Claude Code; those entries have no source files in this repository.

## Built-in Claude Code skills

| Skill                      | When to use                                                                                                               |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `claude-api`               | Building, debugging, or optimising Claude API / Anthropic SDK apps, including prompt caching and model version migrations |
| `fewer-permission-prompts` | Scan transcripts and add a permission allowlist to `.claude/settings.json` to reduce approval prompts                     |
| `init`                     | Initialise a new project with Claude Code conventions                                                                     |
| `keybindings-help`         | Customise keyboard shortcuts, rebind keys, or add chord bindings in `~/.claude/keybindings.json`                          |
| `loop`                     | Run a prompt or slash command on a recurring interval (e.g. `/loop 5m /foo`)                                              |
| `review`                   | Run a code review of the current branch or changes                                                                        |
| `schedule`                 | Create, update, list, or run scheduled remote agents on a cron schedule                                                   |
| `security-review`          | Run a security-focused review of code changes                                                                             |
| `simplify`                 | Review changed code for reuse, quality, and efficiency, then fix issues found                                             |
| `update-config`            | Modify Claude Code configuration via `settings.json`, including hooks, permissions, env vars, and plugins                 |

## Control skill loading

Use `skillOverrides` in `settings.json` to reduce the skills listing and control which skills are available to the model. This is the main tool for reducing token usage, as skill descriptions are the bulk of the listing cost.

| Value                   | Effect                                                                                                        |
| ----------------------- | ------------------------------------------------------------------------------------------------------------- |
| `"on"`                  | Full description shown; the skill is available for automatic and manual use                                   |
| `"name-only"`           | Skill name shown without its description, so it doesn't consume description tokens; `/skill-name` still works |
| `"user-invocable-only"` | Hidden from automatic matching; `/skill-name` still works                                                     |
| `"off"`                 | Hidden from automatic matching and manual use                                                                 |

To re-enable a skill suppressed by a parent settings file, set it to `"on"` in the project or local settings file:

```json
{
	"skillOverrides": {
		"vue": "on",
		"vue-project-stack": "on"
	}
}
```

Project setup does not create a local `.claude/settings.json` by default. Prefer project instructions and `WORKSPACE.md` for repo-specific guidance.

## Add a new skill

1. Create a source folder for the skill, using its slug:

    ```text
    src/skills/my-skill/
    ```

2. Add `skill.json` with the metadata used for discovery and generated tables:

    ```json
    {
    	"name": "my-skill",
    	"title": "My skill",
    	"description": "Use this skill when... (one sentence shown in the skills list)",
    	"when": "When doing this work",
    	"triggers": ["my trigger"],
    	"filePatterns": [],
    	"pathPatterns": [],
    	"dependencies": ["code-style"],
    	"capabilities": {
    		"promptTriggering": true,
    		"fileTriggering": false
    	}
    }
    ```

3. Add `SKILL.body.md` with the instructions:

    ```markdown
    # My skill

    Content here.
    ```

4. Run `scripts/sync.sh` to generate `dist/skills/<name>/SKILL.md`, refresh the skill tables in this file and [docs/commands.md](commands.md), and rebuild the Claude and Codex outputs.

5. Run `bash scripts/validate.sh` to check generated output and markers before you install the skill.

After `scripts/setup-global.sh`, shared skills are available to Claude via `~/.claude/skills/` and to Codex via `~/.agents/skills/`.

## Add an external skill

Use this for official upstream skills that should stay managed externally.

1. Add an entry to `external-skills.json` with `slug`, `name`, `source`, `skill_url`, optional `references_api_url`, optional `commit_api_url`, and `license`.
2. Run `scripts/sync-external-skills.sh`.
3. Add local trigger metadata in `skill.json` if the upstream defaults need adjustment.
4. Run `scripts/sync.sh` to regenerate docs and the Claude and Codex outputs.

Do not edit synced external `SKILL.body.md` files directly. Change the upstream source or local trigger and documentation around it.
