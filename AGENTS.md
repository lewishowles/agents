# Agent configuration repo maintenance

This file is for maintaining this repository. It is a local project instruction file for agents working in this repo; it is not generated by this repo and is not installed as an output of this repo.

The generated outputs are `dist/claude/CLAUDE.md`, `dist/codex/AGENTS.md`, and the ChatGPT files under `dist/chatgpt/`.

Source of truth for Claude Code + OpenAI Codex CLI global configuration. Skills, hooks, and rules live here and are distributed to `~/.claude/`, `~/.agents/`, and `~/.codex/` via symlinks.

## Purpose

Single source for shared rules and skills. `scripts/sync-external-skills.sh` refreshes managed official skills; `scripts/sync.sh` composes agent-specific output files (`dist/claude/CLAUDE.md`, `dist/codex/AGENTS.md`) from content in `src/rules/`.

## Current vs target state

**Current:** `src/` (`src/rules/`, `src/skills/`, `src/hooks/`, `src/adapters/`, `src/fragments/`), `dist/claude/`, `dist/codex/`, `dist/chatgpt/`, `scripts/`, `templates/`, and `docs/`. Dual-target phases are complete; deferred hook ideas remain in root `PROGRESS.md`.

## Target directory structure

```
Configuration/Agents/
├── src/                     # hand-authored source — never author dist/ directly
│   ├── rules/                # canonical shared content
│   │   ├── global-rules.md
│   │   ├── skills-policy.md
│   │   ├── identity.md
│   │   └── file-discovery.md
│   ├── adapters/claude/
│   │   └── settings.base.json  # editable source; dist/claude/settings.json is generated
│   ├── hooks/
│   │   ├── claude/<name>/    # hook source: hook.json + <name>.sh
│   │   └── git/              # this repo's own git hooks (core.hooksPath)
│   ├── skills/                # authored skill sources — single source for both agents
│   │   └── <group>/<name>/    # e.g. vue/vue-pinia/ (flat groups: vue/, swift/, testing/...)
│   │       ├── skill.json     # metadata: name, description, triggers, filePatterns, capabilities
│   │       └── SKILL.body.md  # instructional content (global-rules is the exception — see below)
│   └── fragments/             # per-tool preamble fragments assembled by sync.sh
│       ├── claude/            # header.md, subagent-delegation.md
│       ├── codex/              # header.md
│       └── chatgpt/            # system.md
├── dist/                   # generated output — never author directly, regenerate with sync.sh
│   ├── skills/<name>/SKILL.md  # flattened runtime skill directories
│   ├── claude/
│   │   ├── CLAUDE.md        # generated by sync.sh
│   │   ├── settings.json    # generated by build-settings.py
│   │   └── hooks/           # synced copies of src/hooks/claude/
│   ├── codex/
│   │   └── AGENTS.md        # generated by sync.sh
│   └── chatgpt/             # generated by build-chatgpt-target.py
├── .agent/                  # agent-operated internals (not human-facing contracts)
│   ├── audits/              # ignored reports and audit evidence; never canonical script source
│   ├── specs/               # per-feature specs linked from PROGRESS.md
│   ├── scripts/             # ignored setup-managed symlinks to tracked shared tools
│   └── diagnostics/         # diagnostics logs (when present)
├── .claude/skills/agent-config/  # repo-local maintenance skill
├── .agents/skills/agent-config     # symlink to the repo-local maintenance skill
├── external-skills.json     # managed official upstream skills
├── templates/
│   ├── claude/
│   ├── codex/
│   └── shared/
├── scripts/
│   ├── audit/               # tracked transcript and usage analysis tools
│   ├── sync.sh               # full regen: src/ → dist; calls sub-builders
│   ├── validate.sh           # checks dist/ consistency; exits 0 on pass
│   ├── sync-external-skills.sh
│   ├── setup-global.sh      # --claude|--codex|--both
│   ├── setup-project.sh     # --claude|--codex|--both
│   ├── build/                # build-skill-mds.py, build-docs.py, build-settings.py, build-chatgpt-target.py
│   ├── validate/              # per-check scripts run by validate.sh
│   └── lib/                   # colours.sh, dist-targets.sh, cli-style-output.sh, ...
└── docs/                      # generated reference tables — regenerate with build-docs.py
```

Root holds human-facing contracts that tools auto-discover and the user reads each session (`AGENTS.md`, `PROGRESS.md`, `README.md`); `.agent/` holds agent-operated internals. Keep new files on the correct side of that line.

Durable scripts live under tracked `scripts/`, never under `.agent/`. The project-setup library owns one shared-tool list that links the selected tools into each repository's `.agent/scripts/`; that directory is a projection, not a source location.

## Symlink topology (after `setup-global.sh --both`)

```
~/.claude/CLAUDE.md         → repo/dist/claude/CLAUDE.md
~/.claude/settings.json     → repo/dist/claude/settings.json
~/.claude/skills/<name>     → repo/dist/skills/<name>      (per-skill)
~/.claude/hooks/<file>      → repo/dist/claude/hooks/<file>  (per-hook)
~/.agents/AGENTS.md          → repo/dist/codex/AGENTS.md
~/.agents/skills/<name>      → repo/dist/skills/<name>      (per-skill)
repo/.agents/skills/agent-config → repo/.claude/skills/agent-config (repo-local)
```

## Where to start

- Shared rule content (applies to both Claude Code and Codex) → `src/rules/*.md`
- Claude-only preamble fragments → `src/fragments/claude/`
- Codex-only preamble fragments → `src/fragments/codex/`
- Regenerating `CLAUDE.md`/`AGENTS.md` from source → `scripts/sync.sh`
- A shared or managed skill → `src/skills/<group>/<name>/skill.json` + `SKILL.body.md`
- This repo's own maintenance skill → `.claude/skills/agent-config/SKILL.md`
- Project scaffolding templates (`AGENTS.md`, `WORKSPACE.md` starting points) → `templates/`
- `WORKSPACE.md` generation logic → `scripts/init-workspace.py`
- Project setup / symlink wiring → `scripts/setup-project.sh`, `scripts/setup-global.sh`
- Transcript and usage analysis → `scripts/audit/`; generated reports stay under `.agent/audits/`

## Confirmed Codex behaviour

- **Global rules:** `~/.agents/AGENTS.md`
- **Project rules:** walks root → cwd for `AGENTS.md`; root real file or symlink covers both runtimes
- **Skills:** `~/.agents/skills/` for this machine's global shared skills; frontmatter `name`+`description` visible upfront; full skill loaded on use
- **Skill discovery:** description-driven, not slash-command; `Use this skill when` plus action-led wording and file globs in description = better matching
- **Hooks:** Codex hooks exist but parity is out of scope; skill descriptions carry discovery weight instead

## Edit workflow

Edit source files here in the repo. Symlinks propagate immediately. After editing `src/rules/` or `src/fragments/<agent>/` content, run `scripts/sync.sh` to regenerate `dist/claude/CLAUDE.md` and `dist/codex/AGENTS.md`. Run `scripts/sync-external-skills.sh` to refresh managed official skills. Commit changes here.

Skills listed in `external-skills.json` are upstream-managed: never hand-edit or paraphrase their content under `src/skills/`. Take changes upstream, then re-run `scripts/sync-external-skills.sh`.

Don't edit `~/.claude/` or `~/.agents/` directly — changes won't persist.

## Skill structure

Shared global skills: `src/skills/<group>/<name>/` — flat groups such as `vue/`, `swift/`, `testing/`, `writing/`, `project-management/`. Each skill has:

- `skill.json` — canonical metadata: `name`, `description` (`Use this skill when` + action-led wording + globs, written for both Claude auto-trigger and Codex description-driven discovery), `triggers`, `filePatterns`, `capabilities`, `dependencies`
- `SKILL.body.md` — the editable instructional content

`scripts/build/build-skill-mds.py` generates `dist/skills/<name>/SKILL.md` (frontmatter from `skill.json` + body from `SKILL.body.md`) — never author `dist/skills/` directly. The `global-rules` skill is the one exception: it has no `SKILL.body.md`; its body is composed straight from `src/rules/global-rules.md`, `identity.md`, `skills-policy.md`, and `file-discovery.md` so it can't drift from the source rules.

Managed external skills also live under `src/skills/<name>`, but are listed in `external-skills.json` and include `SYNC.md`. Do not edit their generated `SKILL.md` files directly — edit `SKILL.body.md` and re-run sync.

## Adding a skill

1. Create `src/skills/<group>/<name>/skill.json` + `SKILL.body.md`
2. Run `bash scripts/sync.sh` — regenerates `dist/skills/`, settings, docs, and other dist targets
3. Check `bash scripts/validate.sh` exits 0
4. Re-run `setup-global.sh` if this is a new skill directory, to create its symlink

### Standing context

Verification for any change in this repo:

- `bash scripts/sync.sh` — regenerates all `dist/` output; run after editing anything under `src/`
- `bash scripts/validate.sh` — must exit 0; pipes are misleading, check `$?` directly rather than a `tail`ed exit
- `bash tests/friction-logging.sh` — covers the tool-failure hook, malformed input, a missing `friction` CLI, and the Codex HCOM PATH bootstrap
- `friction doctor --json` — confirms the globally installed friction CLI can open its database
- `friction summary` — groups recurring friction by category, cwd and detail; excludes `check-fail` rows unless you pass `--include-check-fails`

Gotchas:

- `.agent/` is ignored by the global gitignore (`~/.config/git/ignore`), so task files, specs, audits and logs are all untracked. `PROGRESS.md` at the root is the only tracked planning record.
- Editing `src/rules/global-rules.md` grows a file loaded on every turn in every project.
- `scripts/audit/token_usage_report.py` supports two invocation styles: direct script execution (`python3 scripts/audit/token_usage_report.py`) and package import (`from scripts.audit import token_usage_report`, used by `tests/usage-driver-ledger.sh`). Sibling modules (`metrics.py`, `redundancy.py`, and `token_usage_types.py`, `tool_call_attribution.py`, `token_usage_parsing.py`, `token_usage_rendering.py`) must be imported with plain absolute imports after the file's `sys.path.insert(0, str(AUDIT_DIRECTORY))` shim, never relative imports (`from . import ...`) because direct execution runs the report module as `__main__`. This broke twice during the 2026-08-08 craftsmanship refactor.
